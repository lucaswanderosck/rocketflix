import styled from 'styled-components'

export const Container = styled.header`
  text-align: center;

  h1 {
    font-size: 2.5rem;
    font-weight: bold;
  }

  img {
    width: 6rem;
    margin-bottom: 0.25rem;
  }
`
